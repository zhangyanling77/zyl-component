import React from "react";
import { {{headCaps name}} } from "./index";
import {
	withKnobs,
	text,
	boolean,
	color,
	select,
} from "@storybook/addon-knobs";

export default {
	title: "Components/{{headCaps name}}",
	component: {{headCaps name}},
	decorators: [withKnobs],
};

export const knobsBtn = ({{headCaps name}}) => (
);